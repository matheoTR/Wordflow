1. go in Translation_... dir and "pipx install -e ."
This creates an isolated virtual environment just for wordflow, but automatically places the wordflow command in ~/.local/bin folder so we can launch it from anywhere
il faut quand même ajouter ~/.local/bin à PATH 
